# Date Display Classification Draft

| File | Line | Current Text | Proposed | Notes |
|---|---|---|---|---|
| frontend\js\admin-charts.js | 59 | `return date.toLocaleDateString([], { month: 'short', day: 'numeric' });` | UNCERTAIN | 56: <br>57:     const labels = Object.keys(dailyTotals).map((dayKey) => {<br>58:       const date = new Date(`${dayKey}T00:00:00`);<br>59:       return date.toLocaleDateString([], { month: 'short', day: 'numeric' });<br>60:     });<br>61: <br>62:     return { |
| frontend\js\admin-charts.js | 148 | `return `PHP ${Number(context.raw \|\| 0).toLocaleString('en-PH', {` | UNCERTAIN | 145:           tooltip: {<br>146:             callbacks: {<br>147:               label(context) {<br>148:                 return `PHP ${Number(context.raw || 0).toLocaleString('en-PH', {<br>149:                   minimumFractionDigits: 2,<br>150:                   maximumFractionDigits: 2<br>151:                 })}`; |
| frontend\js\admin-charts.js | 171 | `return `PHP ${Number(value).toLocaleString('en-PH')}`;` | UNCERTAIN | 168:             ticks: {<br>169:               font: { size: 10 },<br>170:               callback(value) {<br>171:                 return `PHP ${Number(value).toLocaleString('en-PH')}`;<br>172:               }<br>173:             }<br>174:           } |
| frontend\js\admin.js | 2197 | `<td class="text-muted small">${log.created_at ? new Date(log.created_at).toLocaleString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day` | Date + Time |  |
| frontend\js\admin.js | 2256 | `const time = log.created_at ? new Date(log.created_at).toLocaleString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric', hour:` | Date + Time |  |
| frontend\js\admin.js | 2558 | `if (period === 'year' \|\| period === 'all') return dt.toLocaleString('default', { month: 'short' });` | UNCERTAIN | 2555:             const labels = data.map(d => {<br>2556:                 const dt = new Date(d.label);<br>2557:                 if (period === 'today') return dt.getHours() + ':00';<br>2558:                 if (period === 'year' || period === 'all') return dt.toLocaleString('default', { month: 'short' });<br>2559:                 return dt.toLocaleDateString('default', { month: 'short', day: 'numeric' });<br>2560:             });<br>2561:  |
| frontend\js\admin.js | 2559 | `return dt.toLocaleDateString('default', { month: 'short', day: 'numeric' });` | UNCERTAIN | 2556:                 const dt = new Date(d.label);<br>2557:                 if (period === 'today') return dt.getHours() + ':00';<br>2558:                 if (period === 'year' || period === 'all') return dt.toLocaleString('default', { month: 'short' });<br>2559:                 return dt.toLocaleDateString('default', { month: 'short', day: 'numeric' });<br>2560:             });<br>2561: <br>2562:             this._reportsChart = new ApexCharts(el, { |
| frontend\js\admin.js | 2599 | `formatter: (v, { seriesIndex }) => seriesIndex === 0 ? '₱' + Number(v).toLocaleString() : String(Math.round(v))` | UNCERTAIN | 2596:                     shared: true,<br>2597:                     intersect: false,<br>2598:                     y: {<br>2599:                         formatter: (v, { seriesIndex }) => seriesIndex === 0 ? '₱' + Number(v).toLocaleString() : String(Math.round(v))<br>2600:                     }<br>2601:                 },<br>2602:             }); |
| frontend\js\admin.js | 3056 | `<td class="text-muted">${user.created_at ? new Date(user.created_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day` | Date + Time |  |
| frontend\js\admin.js | 3195 | `<td class="text-muted">${user.created_at ? new Date(user.created_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day` | Date + Time |  |
| frontend\js\admin.js | 3377 | `<td class="text-muted">${user.created_at ? new Date(user.created_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day` | Date + Time |  |
| frontend\js\admin.js | 3609 | `<td class="text-muted">${user.flagged_at ? new Date(user.flagged_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day` | UNCERTAIN | 3606:                     <td>${this.escapeHtml(user.role || '—')}</td><br>3607:                     <td class="text-muted">${this.escapeHtml(user.phone || '—')}</td><br>3608:                     <td>${this.escapeHtml(user.flag_reason || '—')}</td><br>3609:                     <td class="text-muted">${user.flagged_at ? new Date(user.flagged_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric' }) : '—'}</td><br>3610:                     <td class="text-muted">${user.flagged_by_username || '—'}</td><br>3611:                     <td><br>3612:                         <div class="d-flex gap-1"> |
| frontend\js\admin.js | 3781 | `<small class="text-muted">Last updated: ${data.updated_at ? new Date(data.updated_at).toLocaleString('en-PH', { timeZone: 'Asia/Manila' }) : '—'}</small>` | Date + Time |  |
| frontend\js\admin.js | 3793 | `<small class="text-muted">Last updated: ${data.updated_at ? new Date(data.updated_at).toLocaleString('en-PH', { timeZone: 'Asia/Manila' }) : '—'}</small>` | Date + Time |  |
| frontend\js\admin.js | 4450 | `<span class="text-muted" style="font-size:.74rem">${flag.updated_at ? new Date(flag.updated_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila' }) : '—'}<` | Date + Time |  |
| frontend\js\admin.js | 4565 | `lastUpdated.textContent = `Last Updated: ${now.toLocaleTimeString('en-PH', {` | Date + Time |  |
| frontend\js\admin.js | 4688 | `lastUpdated.textContent = `Last Updated: ${now.toLocaleTimeString('en-PH', {` | Date + Time |  |
| frontend\js\admin.js | 4844 | `const time = new Date(activity.timestamp).toLocaleTimeString('en-PH', {` | Date + Time |  |
| frontend\js\admin.js | 4850 | `const date = new Date(activity.timestamp).toLocaleDateString('en-PH', {` | Date + Time |  |
| frontend\js\admin.js | 5210 | `document.getElementById('am-detail-timestamp').textContent = new Date(activity.timestamp).toLocaleString('en-PH', {` | Date + Time |  |
| frontend\js\admin.js | 5258 | `const time = new Date(item.created_at \|\| item.timestamp).toLocaleTimeString('en-PH', {` | Date + Time |  |
| frontend\js\admin.js | 5803 | `const createdLabel = createdAt ? createdAt.toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric' }) : '—';` | Date + Time |  |
| frontend\js\admin.js | 5895 | `['Joined', safeUser.created_at ? new Date(safeUser.created_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'num` | Date Only |  |
| frontend\js\admin.js | 5918 | `<td class="small text-muted">${o.created_at ? new Date(o.created_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day` | Date + Time |  |
| frontend\js\admin.js | 6088 | `['Joined', farmer.created_at ? new Date(farmer.created_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric` | Date Only |  |
| frontend\js\admin.js | 6301 | `<td class="small text-muted">${new Date(a.timestamp).toLocaleString('en-PH', { timeZone: 'Asia/Manila', month: 'short', day: 'numeric', hour: '2-digit', minute:` | Date + Time |  |
| frontend\js\admin.js | 6336 | `['Joined', user.created_at ? new Date(user.created_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric' })` | Date Only |  |
| frontend\js\admin.js | 6430 | `['Joined', user.created_at ? new Date(user.created_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric' })` | Date Only |  |
| frontend\js\admin.js | 7595 | `const created = a.created_at ? new Date(a.created_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric' }) ` | Date Only |  |
| frontend\js\admin.js | 8242 | `${p.is_preorder && p.preorder_availability_date ? `<div class="text-muted" style="font-size:.75rem">Available: ${new Date(p.preorder_availability_date).toLocale` | Date Only |  |
| frontend\js\admin.js | 8248 | `<td class="small text-muted">${p.created_at ? new Date(p.created_at).toLocaleDateString() : '—'}</td>` | Date + Time |  |
| frontend\js\admin.js | 8405 | `<td>${new Date(ticket.created_at).toLocaleDateString('en-PH')}</td>` | Date + Time |  |
| frontend\js\admin.js | 8406 | `<td>${ticket.updated_at ? new Date(ticket.updated_at).toLocaleDateString('en-PH') : '—'}</td>` | Date + Time |  |
| frontend\js\admin.js | 8492 | `document.getElementById('admin-ticket-detail-created').textContent = new Date(ticket.created_at).toLocaleDateString('en-PH');` | Date + Time |  |
| frontend\js\admin.js | 8560 | `const exactTime = new Date(msg.created_at).toLocaleString('en-PH');` | Date + Time |  |
| frontend\js\admin.js | 8848 | `const date   = request.created_at ? new Date(request.created_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'n` | Date + Time |  |
| frontend\js\admin.js | 9592 | `<p>Date: ${new Date(order.created_at).toLocaleString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric', hour: '2-digit', minut` | Date Only |  |
| frontend\js\admin.js | 9593 | `${order.delivery_date ? `<p>Delivery Date: ${new Date(order.delivery_date).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short` | Date Only |  |
| frontend\js\admin.js | 9600 | `${order.items[0].harvest_date ? `<p style="margin:4px 0;font-size:0.875rem;"><strong>Expected Harvest:</strong> ${new Date(order.items[0].harvest_date).toLocale` | UNCERTAIN | 9597:             ${(order.items && order.items[0] && order.items[0].is_preorder) ? `<br>9598:             <div class="panel-section" style="background:#f0fdf4;border-left:3px solid #16a34a;padding:12px;margin-bottom:16px;"><br>9599:                 <h4 style="color:#16a34a;margin:0 0 8px 0;font-size:0.875rem;">Pre-order & Harvest Information</h4><br>9600:                 ${order.items[0].harvest_date ? `<p style="margin:4px 0;font-size:0.875rem;"><strong>Expected Harvest:</strong> ${new Date(order.items[0].harvest_date).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric' })}</p>` : ''}<br>9601:                 <div style="margin:4px 0;font-size:0.875rem;">${this.getHarvestBadgeHtml(order.items[0].harvest_date, null)}</div><br>9602:                 <hr style="margin:8px 0;border:none;border-top:1px solid #d1d5db;"><br>9603:                 <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:4px 0;font-size:0.875rem;"> |
| frontend\js\admin.js | 9609 | `${order.preorder_availability_date ? `<p style="margin:4px 0;font-size:0.875rem;"><strong>Availability Date:</strong> ${new Date(order.preorder_availability_dat` | Date Only |  |
| frontend\js\admin.js | 9610 | `${order.items[0].harvest_date_updated_at ? `<p style="margin:4px 0;font-size:0.875rem;"><strong>Last Updated:</strong> ${new Date(order.items[0].harvest_date_up` | Date Only |  |
| frontend\js\admin.js | 9612 | `${order.items[0].previous_harvest_date ? `<p style="margin:4px 0;font-size:0.875rem;"><strong>Previous Harvest Date:</strong> ${new Date(order.items[0].previous` | Date Only |  |
| frontend\js\admin.js | 10369 | `<div class="fw-semibold">${new Date(product.harvest_date).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric'` | Date Only |  |
| frontend\js\admin.js | 10395 | `<div class="fw-semibold">${new Date(product.preorder_availability_date).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', ` | Date Only |  |
| frontend\js\admin.js | 10789 | `const time = entry.created_at ? new Date(entry.created_at).toLocaleTimeString('en-PH', { timeZone: 'Asia/Manila', hour: '2-digit', minute: '2-digit' }) : '';` | Date + Time |  |
| frontend\js\admin.js | 10790 | `const date = entry.created_at ? new Date(entry.created_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', month: 'short', day: 'numeric' }) : '';` | Date + Time |  |
| frontend\js\admin.js | 10946 | `<td class="text-muted">${order.created_at ? new Date(order.created_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', d` | Date + Time |  |
| frontend\js\admin.js | 11138 | `<td class="text-muted">${f.created_at ? new Date(f.created_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'num` | Date + Time |  |
| frontend\js\admin.js | 11784 | `<td>${new Date(request.created_at).toLocaleDateString()}</td>` | Date + Time |  |
| frontend\js\admin.js | 11997 | `<tr><td><strong>Submitted:</strong></td><td>${new Date(request.created_at).toLocaleString()}</td></tr>` | Date + Time |  |
| frontend\js\admin.js | 11998 | `<tr><td><strong>Reviewed:</strong></td><td>${request.reviewed_at ? new Date(request.reviewed_at).toLocaleString() : '—'}</td></tr>` | Date + Time |  |
| frontend\js\admin.js | 12201 | `<td>₱${Number(s.amount_paid \|\| 0).toLocaleString()}</td>` | Date + Time |  |
| frontend\js\admin.js | 12203 | `<td>${new Date(s.created_at).toLocaleDateString('en-PH')}</td>` | Date + Time |  |
| frontend\js\admin.js | 12245 | `document.getElementById('sub-detail-amount').textContent = `₱${Number(subscription.amount_paid \|\| 0).toLocaleString()}`;` | UNCERTAIN | 12242:             document.getElementById('sub-detail-email').textContent = subscription.email || '—';<br>12243:             document.getElementById('sub-detail-shop-name').textContent = subscription.shop_name || '—';<br>12244:             document.getElementById('sub-detail-plan').textContent = subscription.tier || '—';<br>12245:             document.getElementById('sub-detail-amount').textContent = `₱${Number(subscription.amount_paid || 0).toLocaleString()}`;<br>12246:             document.getElementById('sub-detail-duration').textContent = `${subscription.plan_duration_months} month${subscription.plan_duration_months > 1 ? 's' : ''}`;<br>12247:             document.getElementById('sub-detail-payment-to').textContent = subscription.payment_account_name || '—';<br>12248:             document.getElementById('sub-detail-account-number').textContent = subscription.payment_account_number || '—'; |
| frontend\js\admin.js | 12273 | `document.getElementById('sub-detail-created-at').textContent = new Date(subscription.created_at).toLocaleDateString('en-PH');` | Date Only |  |
| frontend\js\admin.js | 12279 | `document.getElementById('sub-detail-starts-at').textContent = subscription.starts_at ? new Date(subscription.starts_at).toLocaleDateString('en-PH') : '—';` | Date Only |  |
| frontend\js\admin.js | 12280 | `document.getElementById('sub-detail-expires-at').textContent = subscription.expires_at ? new Date(subscription.expires_at).toLocaleDateString('en-PH') : '—';` | Date Only |  |
| frontend\js\app.js | 5838 | `const harvestFormatted = new Date(product.harvest_date).toLocaleDateString('en-PH', { month: 'short', day: 'numeric', year: 'numeric' });` | UNCERTAIN | 5835:                     harvestStatus = `${diffDays} Days`;<br>5836:                     harvestStatusClass = 'text-warning';<br>5837:                 } else if (diffDays > 3) {<br>5838:                     const harvestFormatted = new Date(product.harvest_date).toLocaleDateString('en-PH', { month: 'short', day: 'numeric', year: 'numeric' });<br>5839:                     harvestStatus = harvestFormatted;<br>5840:                     harvestStatusClass = 'text-primary';<br>5841:                 } else { |
| frontend\js\app.js | 5861 | `? new Date(product.expiry_date).toLocaleDateString('en-PH', { month: 'short', day: 'numeric' })` | Date Only |  |
| frontend\js\app.js | 6193 | `harvestDate = harvest.toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'long', day: 'numeric' });` | Date Only |  |
| frontend\js\app.js | 6211 | `expiryDate = expiry.toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'long', day: 'numeric' });` | Date Only |  |
| frontend\js\chat.js | 395 | `? new Date(meta.created_at).toLocaleDateString('en-PH', {` | Date + Time |  |
| frontend\js\chat.js | 1023 | `dayLabel = firstMsgDate.toLocaleDateString('en-US', {` | UNCERTAIN | 1020:                 } else if (diffDays === 1) {<br>1021:                     dayLabel = 'Yesterday';<br>1022:                 } else if (diffDays < 7) {<br>1023:                     dayLabel = firstMsgDate.toLocaleDateString('en-US', {<br>1024:                         timeZone: 'Asia/Manila',<br>1025:                         weekday: 'long'<br>1026:                     }); |
| frontend\js\chat.js | 1028 | `dayLabel = firstMsgDate.toLocaleDateString('en-US', {` | UNCERTAIN | 1025:                         weekday: 'long'<br>1026:                     });<br>1027:                 } else {<br>1028:                     dayLabel = firstMsgDate.toLocaleDateString('en-US', {<br>1029:                         timeZone: 'Asia/Manila',<br>1030:                         month: 'short',<br>1031:                         day: 'numeric' |
| frontend\js\chat.js | 1247 | `dayLabel = firstMsgDate.toLocaleDateString('en-US', {` | UNCERTAIN | 1244:                 } else if (diffDays === 1) {<br>1245:                     dayLabel = 'Yesterday';<br>1246:                 } else if (diffDays < 7) {<br>1247:                     dayLabel = firstMsgDate.toLocaleDateString('en-US', {<br>1248:                         timeZone: 'Asia/Manila',<br>1249:                         weekday: 'long'<br>1250:                     }); |
| frontend\js\chat.js | 1252 | `dayLabel = firstMsgDate.toLocaleDateString('en-US', {` | UNCERTAIN | 1249:                         weekday: 'long'<br>1250:                     });<br>1251:                 } else {<br>1252:                     dayLabel = firstMsgDate.toLocaleDateString('en-US', {<br>1253:                         timeZone: 'Asia/Manila',<br>1254:                         month: 'short',<br>1255:                         day: 'numeric' |
| frontend\js\chat.js | 1598 | `return d.toLocaleDateString('en-US', {` | Date + Time |  |
| frontend\js\chat.js | 1623 | `const timeStr = d.toLocaleTimeString('en-US', {` | UNCERTAIN | 1620:         const msgDate = new Date(d);<br>1621:         msgDate.setHours(0, 0, 0, 0);<br>1622: <br>1623:         const timeStr = d.toLocaleTimeString('en-US', {<br>1624:             timeZone: 'Asia/Manila',<br>1625:             hour: 'numeric',<br>1626:             minute: '2-digit', |
| frontend\js\chat.js | 1637 | `const dayName = d.toLocaleDateString('en-US', {` | UNCERTAIN | 1634:             return `Yesterday at ${timeStr}`;<br>1635:         }<br>1636:         if (diffDays < 7) {<br>1637:             const dayName = d.toLocaleDateString('en-US', {<br>1638:                 timeZone: 'Asia/Manila',<br>1639:                 weekday: 'long'<br>1640:             }); |
| frontend\js\chat.js | 1647 | `return d.toLocaleDateString('en-US', {` | UNCERTAIN | 1644:         const currentYear = now.getFullYear();<br>1645:         const msgYear = d.getFullYear();<br>1646:         if (msgYear === currentYear) {<br>1647:             return d.toLocaleDateString('en-US', {<br>1648:                 timeZone: 'Asia/Manila',<br>1649:                 month: 'short',<br>1650:                 day: 'numeric' |
| frontend\js\chat.js | 1654 | `return d.toLocaleDateString('en-US', {` | UNCERTAIN | 1651:             }) + ` at ${timeStr}`;<br>1652:         }<br>1653: <br>1654:         return d.toLocaleDateString('en-US', {<br>1655:             timeZone: 'Asia/Manila',<br>1656:             year: 'numeric',<br>1657:             month: 'short', |
| frontend\js\customer-account.js | 298 | `const joined = profile.created_at ? new Date(profile.created_at).toLocaleDateString() : '—';` | Date Only |  |
| frontend\js\customer-account.js | 800 | `if (displaySubmitted) displaySubmitted.textContent = request.created_at ? new Date(request.created_at).toLocaleString() : '—';` | Date + Time |  |
| frontend\js\customer-account.js | 911 | `<td>${new Date(ticket.created_at).toLocaleDateString('en-PH')}</td>` | Date + Time |  |
| frontend\js\customer-account.js | 1248 | `<td>${new Date(request.created_at).toLocaleDateString()}</td>` | Date + Time |  |
| frontend\js\farmer.js | 1526 | `const when = new Date(r.created_at).toLocaleString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric', hour: '2-digit', minute:` | Date + Time |  |
| frontend\js\farmer.js | 1597 | `const when = new Date(r.created_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric' });` | Date + Time |  |
| frontend\js\farmer.js | 1652 | `const when = new Date(product.created_at).toLocaleString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric', hour: '2-digit', m` | Date + Time |  |
| frontend\js\farmer.js | 4830 | `? new Date(user.created_at).toLocaleDateString('en-PH', { year: 'numeric', month: 'short', day: 'numeric' })` | Date Only |  |
| frontend\js\farmer.js | 4954 | `const date = new Date(sub.created_at).toLocaleDateString('en-PH', { year: 'numeric', month: 'short', day: 'numeric' });` | Date + Time |  |
| frontend\js\farmer.js | 4956 | `const amount = sub.amount_paid ? `₱${parseFloat(sub.amount_paid).toLocaleString()}` : '—';` | Date + Time |  |
| frontend\js\farmer.js | 4963 | `const start = new Date(sub.starts_at).toLocaleDateString('en-PH', { month: 'short', day: 'numeric' });` | Date Only |  |
| frontend\js\farmer.js | 4964 | `const end = new Date(sub.expires_at).toLocaleDateString('en-PH', { month: 'short', day: 'numeric', year: 'numeric' });` | Date Only |  |
| frontend\js\farmer.js | 5045 | `<div>₱${Number(subscription.amount_paid \|\| 0).toLocaleString()}</div>` | UNCERTAIN | 5042:                     </div><br>5043:                     <div class="col-md-6"><br>5044:                         <label class="small fw-semibold text-muted">Amount Paid</label><br>5045:                         <div>₱${Number(subscription.amount_paid || 0).toLocaleString()}</div><br>5046:                     </div><br>5047:                     <div class="col-md-6"><br>5048:                         <label class="small fw-semibold text-muted">Status</label> |
| frontend\js\farmer.js | 5053 | `<div>${new Date(subscription.created_at).toLocaleDateString('en-PH', { year: 'numeric', month: 'long', day: 'numeric' })}</div>` | Date Only |  |
| frontend\js\farmer.js | 5058 | `<div>${new Date(subscription.starts_at).toLocaleDateString('en-PH', { year: 'numeric', month: 'long', day: 'numeric' })}</div>` | Date Only |  |
| frontend\js\farmer.js | 5064 | `<div>${new Date(subscription.expires_at).toLocaleDateString('en-PH', { year: 'numeric', month: 'long', day: 'numeric' })}</div>` | Date Only |  |
| frontend\js\farmer.js | 5126 | `if (expEl && d.expires_at) expEl.textContent = new Date(d.expires_at).toLocaleDateString('en-PH',{year:'numeric',month:'long',day:'numeric'});` | Date Only |  |
| frontend\js\farmer.js | 5212 | `<p class="fw-bold mb-1" style="color:var(--ac-primary,#2d7a3a);">₱${info.total.toLocaleString()}</p>` | UNCERTAIN | 5209:             col.innerHTML = `<div class="card h-100 plan-option ${borderClass} ${bgClass}" data-months="${m}" style="border-width:2px; border-color:${isDefault ? 'var(--ac-primary,#2d7a3a)' : 'var(--ac-border)'};"><br>5210:                 <div class="card-body text-center"><br>5211:                     <h5>${info.months} Month${info.months>1?'s':''}</h5><br>5212:                     <p class="fw-bold mb-1" style="color:var(--ac-primary,#2d7a3a);">₱${info.total.toLocaleString()}</p><br>5213:                     ${info.discount_pct>0?`<small style="color:var(--ac-primary-dark,#1e5429);">Save ${info.discount_pct}%</small>`:''}<br>5214:                 </div></div>`;<br>5215:             col.querySelector('.plan-option').addEventListener('click', () => { |
| frontend\js\farmer.js | 5224 | `document.getElementById('sub-amount-total').textContent = `₱${info.total.toLocaleString()}`;` | Date Only |  |
| frontend\js\farmer.js | 5231 | `document.getElementById('sub-amount-total').textContent = `₱${settings.durations[1].total.toLocaleString()}`;` | UNCERTAIN | 5228:             opts.appendChild(col);<br>5229:         });<br>5230:         this.selectedDuration = 1;<br>5231:         document.getElementById('sub-amount-total').textContent = `₱${settings.durations[1].total.toLocaleString()}`;<br>5232:         document.getElementById('sub-amount-total').dataset.amount = settings.durations[1].total;<br>5233:         const title = document.getElementById('subscription-modal-title');<br>5234:         const currentInfo = document.getElementById('subscription-current-info'); |
| frontend\js\farmer.js | 5238 | `document.getElementById('sub-current-expiry').textContent = new Date(this.subscriptionData.expires_at).toLocaleDateString('en-PH');` | Date Only |  |
| frontend\js\farmer.js | 5262 | `if (el) el.textContent = newExpiry.toLocaleDateString('en-PH', { year: 'numeric', month: 'long', day: 'numeric' });` | Date Only |  |
| frontend\js\farmer.js | 6437 | `<div style="font-size:0.75rem;color:#9ca3af;margin-top:3px;">${n.created_at ? new Date(n.created_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year` | Date + Time |  |
| frontend\js\farmer.js | 6625 | `lastUpdatedEl.textContent = `${periodLabel} • Updated: ${ts.toLocaleString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric', ` | Date + Time |  |
| frontend\js\farmer.js | 6671 | `lastUpdatedEl.textContent = `Basic View • Updated: ${ts.toLocaleString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric', hour` | Date + Time |  |
| frontend\js\farmer.js | 7000 | `if (period === 'year' \|\| period === 'all') return dt.toLocaleString('default', { month: 'short' });` | UNCERTAIN | 6997:                 const labels = data.map(d => {<br>6998:                     const dt = new Date(d.label);<br>6999:                     if (period === 'today') return dt.getHours() + ':00';<br>7000:                     if (period === 'year' || period === 'all') return dt.toLocaleString('default', { month: 'short' });<br>7001:                     return dt.toLocaleDateString('default', { month: 'short', day: 'numeric' });<br>7002:                 });<br>7003:  |
| frontend\js\farmer.js | 7001 | `return dt.toLocaleDateString('default', { month: 'short', day: 'numeric' });` | UNCERTAIN | 6998:                     const dt = new Date(d.label);<br>6999:                     if (period === 'today') return dt.getHours() + ':00';<br>7000:                     if (period === 'year' || period === 'all') return dt.toLocaleString('default', { month: 'short' });<br>7001:                     return dt.toLocaleDateString('default', { month: 'short', day: 'numeric' });<br>7002:                 });<br>7003: <br>7004:                 this.overviewCharts.reports = new ApexCharts(el, { |
| frontend\js\farmer.js | 7051 | `formatter: (v, { seriesIndex }) => seriesIndex === 0 ? '₱' + Number(v).toLocaleString() : String(Math.round(v))` | UNCERTAIN | 7048:                         shared: true,<br>7049:                         intersect: false,<br>7050:                         y: {<br>7051:                             formatter: (v, { seriesIndex }) => seriesIndex === 0 ? '₱' + Number(v).toLocaleString() : String(Math.round(v))<br>7052:                         }<br>7053:                     },<br>7054:                     states: { |
| frontend\js\farmer.js | 7459 | `const createdLabel = createdAt ? createdAt.toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric' }) : '—';` | Date + Time |  |
| frontend\js\farmer.js | 7689 | `<td class="small">${product.harvest_date ? new Date(product.harvest_date).toLocaleDateString() : 'N/A'}</td>` | UNCERTAIN | 7686:                         ${reservationIndicator}<br>7687:                     </td><br>7688:                     <td class="small">${this.escapeHtml(product.category_name || 'N/A')}</td><br>7689:                     <td class="small">${product.harvest_date ? new Date(product.harvest_date).toLocaleDateString() : 'N/A'}</td><br>7690:                     <td><br>7691:                         ${progressPercent >= 100<br>7692:                             ? `<div class="small fw-bold text-success"><i class="bi bi-check-circle-fill me-1"></i>Reserved Full</div>` |
| frontend\js\farmer.js | 8126 | `const date = r.created_at ? new Date(r.created_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric' }) : '` | Date + Time |  |
| frontend\js\farmer.js | 8200 | `const harvestDate = product.harvest_date ? new Date(product.harvest_date).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short'` | Date Only |  |
| frontend\js\farmer.js | 8201 | `const expiryDate = product.expiry_date ? new Date(product.expiry_date).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', d` | Date Only |  |
| frontend\js\farmer.js | 10124 | `const deliveryDate = order.delivery_date ? new Date(order.delivery_date).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short',` | Date Only |  |
| frontend\js\farmer.js | 10132 | `? orderDate.toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric' })` | UNCERTAIN | 10129:             const customerTotalRatings = Number(order.customer_total_ratings || 0);<br>10130:             const searchText = `${String(orderId)} ${productName} ${customerName}`.toLowerCase();<br>10131:             const dateLabel = orderDate && !Number.isNaN(orderDate.getTime())<br>10132:                 ? orderDate.toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric' })<br>10133:                 : '—';<br>10134:             <br>10135:             return ` |
| frontend\js\farmer.js | 10224 | `const deliveryDate = order.delivery_date ? new Date(order.delivery_date).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short',` | Date Only |  |
| frontend\js\farmer.js | 10225 | `const orderDate = order.created_at ? new Date(order.created_at).toLocaleString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeri` | Date Only |  |
| frontend\js\farmer.js | 10241 | `<div class="order-info-value">${this.escapeHtml(harvestDateObj.toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'num` | Date Only |  |
| frontend\js\farmer.js | 10255 | `<div class="order-info-value">${this.escapeHtml(expiryDateObj.toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'nume` | Date Only |  |
| frontend\js\farmer.js | 10399 | `const orderDate = order.created_at ? new Date(order.created_at).toLocaleString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeri` | Date Only |  |
| frontend\js\farmer.js | 10400 | `const confirmedDate = order.confirmed_at ? new Date(order.confirmed_at).toLocaleString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day:` | Date Only |  |
| frontend\js\farmer.js | 10401 | `const preparedDate = order.prepared_at ? new Date(order.prepared_at).toLocaleString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'n` | Date Only |  |
| frontend\js\farmer.js | 10402 | `const outForDeliveryDate = order.out_for_delivery_at ? new Date(order.out_for_delivery_at).toLocaleString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', m` | Date Only |  |
| frontend\js\farmer.js | 10403 | `const scheduledDate = order.scheduled_delivery_date ? new Date(order.scheduled_delivery_date).toLocaleString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric'` | Date Only |  |
| frontend\js\farmer.js | 10404 | `const deliveredDate = order.delivered_at ? new Date(order.delivered_at).toLocaleString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day:` | Date Only |  |
| frontend\js\farmer.js | 10440 | `if (step.status === 'confirmed') return { title: 'Order Cancelled', time: order.cancelled_at ? new Date(order.cancelled_at).toLocaleString('en-PH', { timeZone: ` | Date + Time |  |
| frontend\js\farmer.js | 11332 | `document.getElementById('display-submitted-date').textContent = new Date(this.currentVerificationRequest.created_at).toLocaleDateString();` | Date + Time |  |
| frontend\js\farmer.js | 11392 | `<td>${new Date(request.created_at).toLocaleDateString('en-PH', { year: 'numeric', month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit' })}</td>` | Date + Time |  |
| frontend\js\farmer.js | 11605 | `<td>${new Date(ticket.created_at).toLocaleDateString('en-PH')}</td>` | Date + Time |  |
| frontend\js\farmers.js | 79 | `<span>${new Date(farmer.created_at).toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric' })}</span>` | Date + Time |  |
| frontend\js\format.js | 79 | `async function formatDate(dateInput, options = {}) {` | Date + Time |  |
| frontend\js\format.js | 95 | `return date.toLocaleString('en-PH', defaultOptions);` | UNCERTAIN | 92:       ...options<br>93:     };<br>94:     <br>95:     return date.toLocaleString('en-PH', defaultOptions);<br>96:   }<br>97: <br>98:   // Format date only (no time) |
| frontend\js\format.js | 107 | `return formatDate(dateInput, defaultOptions);` | UNCERTAIN | 104:       day: 'numeric',<br>105:       ...options<br>106:     };<br>107:     return formatDate(dateInput, defaultOptions);<br>108:   }<br>109: <br>110:   // Format time only (no date) |
| frontend\js\format.js | 118 | `return formatDate(dateInput, defaultOptions);` | UNCERTAIN | 115:       minute: '2-digit',<br>116:       ...options<br>117:     };<br>118:     return formatDate(dateInput, defaultOptions);<br>119:   }<br>120: <br>121:   // Get current server time as Date object |
| frontend\js\notifications-page.js | 157 | `const date = n.created_at ? new Date(n.created_at).toLocaleString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', day: 'numeric', hour: '2-` | Date + Time |  |
| frontend\js\orders.js | 568 | `const displayDate = Number.isNaN(createdAt.getTime()) ? '—' : createdAt.toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'short', ` | Date + Time |  |
| frontend\js\orders.js | 569 | `const displayTime = Number.isNaN(createdAt.getTime()) ? '' : createdAt.toLocaleTimeString('en-PH', { timeZone: 'Asia/Manila', hour: '2-digit', minute: '2-digit'` | Date + Time |  |
| frontend\js\orders.js | 606 | `const d = dt.toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', month: 'short', day: 'numeric' });` | UNCERTAIN | 603: <br>604:             const fmtTimelineDate = (dt) => {<br>605:                 if (!dt || Number.isNaN(dt.getTime())) return '';<br>606:                 const d = dt.toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', month: 'short', day: 'numeric' });<br>607:                 const t = dt.toLocaleTimeString('en-PH', { timeZone: 'Asia/Manila', hour: '2-digit', minute: '2-digit' });<br>608:                 return `${d}, ${t}`;<br>609:             }; |
| frontend\js\orders.js | 607 | `const t = dt.toLocaleTimeString('en-PH', { timeZone: 'Asia/Manila', hour: '2-digit', minute: '2-digit' });` | UNCERTAIN | 604:             const fmtTimelineDate = (dt) => {<br>605:                 if (!dt || Number.isNaN(dt.getTime())) return '';<br>606:                 const d = dt.toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', month: 'short', day: 'numeric' });<br>607:                 const t = dt.toLocaleTimeString('en-PH', { timeZone: 'Asia/Manila', hour: '2-digit', minute: '2-digit' });<br>608:                 return `${d}, ${t}`;<br>609:             };<br>610:  |
| frontend\js\orders.js | 656 | `${isPreorder && item.harvest_date ? `<div class="order-item-meta"><strong>Expected Harvest:</strong> ${new Date(item.harvest_date).toLocaleDateString('en-PH', {` | Date Only |  |
| frontend\js\orders.js | 657 | `${isPreorder && item.harvest_date_updated_at ? `<div class="order-item-meta"><strong>Last Updated:</strong> ${new Date(item.harvest_date_updated_at).toLocaleDat` | Date Only |  |
| frontend\js\orders.js | 659 | `${isPreorder && item.previous_harvest_date ? `<div class="order-item-meta"><strong>Previous Harvest Date:</strong> ${new Date(item.previous_harvest_date).toLoca` | Date Only |  |
| frontend\js\orders.js | 661 | `${isPreorder && item.preorder_availability_date ? `<div class="order-item-meta"><strong>Harvest Date:</strong> ${new Date(item.preorder_availability_date).toLoc` | Date Only |  |
| frontend\js\orders.js | 665 | `${order.delivery_date ? `<div class="order-item-meta"><strong>Delivery Date Announced:</strong> ${new Date(order.delivery_date).toLocaleDateString('en-PH', { ti` | Date Only |  |
| frontend\js\support-ticket-chat.js | 448 | `dayLabel = firstMsgDate.toLocaleDateString('en-US', {` | UNCERTAIN | 445:                 } else if (diffDays === 1) {<br>446:                     dayLabel = 'Yesterday';<br>447:                 } else if (diffDays < 7) {<br>448:                     dayLabel = firstMsgDate.toLocaleDateString('en-US', {<br>449:                         timeZone: 'Asia/Manila',<br>450:                         weekday: 'long'<br>451:                     }); |
| frontend\js\support-ticket-chat.js | 453 | `dayLabel = firstMsgDate.toLocaleDateString('en-US', {` | UNCERTAIN | 450:                         weekday: 'long'<br>451:                     });<br>452:                 } else {<br>453:                     dayLabel = firstMsgDate.toLocaleDateString('en-US', {<br>454:                         timeZone: 'Asia/Manila',<br>455:                         month: 'short',<br>456:                         day: 'numeric' |
| frontend\js\support-ticket-chat.js | 517 | `return d.toLocaleDateString('en-US', {` | Date + Time |  |
| frontend\js\support-ticket-chat.js | 542 | `const timeStr = d.toLocaleTimeString('en-US', {` | UNCERTAIN | 539:         const msgDate = new Date(d);<br>540:         msgDate.setHours(0, 0, 0, 0);<br>541: <br>542:         const timeStr = d.toLocaleTimeString('en-US', {<br>543:             timeZone: 'Asia/Manila',<br>544:             hour: 'numeric',<br>545:             minute: '2-digit', |
| frontend\js\support-ticket-chat.js | 556 | `const dayName = d.toLocaleDateString('en-US', {` | UNCERTAIN | 553:             return `Yesterday at ${timeStr}`;<br>554:         }<br>555:         if (diffDays < 7) {<br>556:             const dayName = d.toLocaleDateString('en-US', {<br>557:                 timeZone: 'Asia/Manila',<br>558:                 weekday: 'long'<br>559:             }); |
| frontend\js\support-ticket-chat.js | 562 | `return d.toLocaleDateString('en-US', {` | UNCERTAIN | 559:             });<br>560:             return `${dayName} at ${timeStr}`;<br>561:         }<br>562:         return d.toLocaleDateString('en-US', {<br>563:             timeZone: 'Asia/Manila',<br>564:             month: 'short',<br>565:             day: 'numeric', |
| tests\preorder-data-persistence-verification.spec.js | 254 | `const localDateStr = dbDate.toLocaleDateString('en-CA', { year: 'numeric', month: '2-digit', day: '2-digit' }); // YYYY-MM-DD format` | Date Only |  |
| tests\test-harvest-reminder-customer.js | 21 | `const harvestFormatted = harvest.toLocaleDateString('en-PH', { month: 'short', day: 'numeric', year: 'numeric' });` | UNCERTAIN | 18:     } else if (diffDays > 0 && diffDays <= 3) {<br>19:         return { status: `${diffDays} Days`, class: 'text-warning' };<br>20:     } else if (diffDays > 3) {<br>21:         const harvestFormatted = harvest.toLocaleDateString('en-PH', { month: 'short', day: 'numeric', year: 'numeric' });<br>22:         return { status: harvestFormatted, class: 'text-primary' };<br>23:     } else {<br>24:         return { status: 'To Be Announced', class: 'text-muted' }; |
| tests\test-harvest-reminder-customer.js | 42 | `{ date: in7Days, expected: in7Days.toLocaleDateString('en-PH', { month: 'short', day: 'numeric', year: 'numeric' }) },` | UNCERTAIN | 39:     { date: today, expected: 'Today' },<br>40:     { date: tomorrow, expected: '1 Days' },<br>41:     { date: in3Days, expected: '3 Days' },<br>42:     { date: in7Days, expected: in7Days.toLocaleDateString('en-PH', { month: 'short', day: 'numeric', year: 'numeric' }) },<br>43:     { date: yesterday, expected: 'To Be Announced' }<br>44: ];<br>45:  |
| tests\test-harvest-reminder-customer.js | 105 | `const harvestFormatted = harvest.toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'long', day: 'numeric' });` | UNCERTAIN | 102:         } else if (diffDays > 0 && diffDays <= 3) {<br>103:             return { display: `${diffDays} Days`, class: 'text-warning' };<br>104:         } else if (diffDays > 3) {<br>105:             const harvestFormatted = harvest.toLocaleDateString('en-PH', { timeZone: 'Asia/Manila', year: 'numeric', month: 'long', day: 'numeric' });<br>106:             return { display: harvestFormatted, class: 'text-primary' };<br>107:         } else {<br>108:             return { display: 'To Be Announced', class: 'text-muted' }; |
| tests\test-harvest-reminder-customer.js | 118 | `{ product: { is_preorder: true, harvest_date: in7Days.toISOString() }, expected: in7Days.toLocaleDateString('en-PH', { month: 'long', day: 'numeric', year: 'num` | UNCERTAIN | 115: const modalTestCases = [<br>116:     { product: { is_preorder: true, harvest_date: today.toISOString() }, expected: 'Today' },<br>117:     { product: { is_preorder: true, harvest_date: tomorrow.toISOString() }, expected: '1 Days' },<br>118:     { product: { is_preorder: true, harvest_date: in7Days.toISOString() }, expected: in7Days.toLocaleDateString('en-PH', { month: 'long', day: 'numeric', year: 'numeric' }) },<br>119:     { product: { is_preorder: true, harvest_date: yesterday.toISOString() }, expected: 'To Be Announced' },<br>120:     { product: { is_preorder: true, harvest_date: null }, expected: 'To Be Announced' },<br>121:     { product: { is_preorder: false, harvest_date: today.toISOString() }, expected: 'Not applicable for non-preorder' } |
