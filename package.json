{
  "name": "agricatch",
  "private": true,
  "scripts": {
    "dev": "npm --prefix backend run dev",
    "start": "npm --prefix backend run start",
    "install:backend": "npm --prefix backend install",
    "purge:fishery:dry": "node backend/scripts/purge-fishery.js --dry-run --verbose",
    "purge:fishery": "node backend/scripts/purge-fishery.js --apply --verbose"
  }
}
